import MessageDetails from "@/components/admin/MessageDetails";
import React from "react";

const page = () => {
  return (
    <div>
      <MessageDetails />
    </div>
  );
};

export default page;
